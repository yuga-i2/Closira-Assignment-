version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./closira.db
      - LOG_LEVEL=INFO
      - APP_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
